#include <stdio.h>
#include <stdlib.h>
#include <time.h>

/**
 * main - prints lower then upper.
 * Return: Always 0.
 */

int main(void)

{
	char low;

	while (low = 'a'; low <='z'; low++)
		putchar(low);

	while (low = 'A'; low <= 'Z'; low++)
		putchar(low);
	putchar('\n');

	Return (0);
}
