#include "main.h"

/*
 * _abs - gives the absolute value of an integer.
 * @n: the integer
 * Return: the absolute value of int n
 */

int _abs(int n)
{
	if (n >= 0)
	{
		return (n);
	}
	return (-n);
}
